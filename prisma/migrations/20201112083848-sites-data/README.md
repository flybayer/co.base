# Migration `20201112083848-sites-data`

This migration has been generated by Eric Vicenti at 11/12/2020, 12:38:48 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Site" (
"id" SERIAL,
"name" text   NOT NULL ,
"ownerId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."SiteNode" (
"id" SERIAL,
"key" text   NOT NULL ,
"siteId" integer   NOT NULL ,
"parentNodeId" integer   ,
"schema" jsonb   ,
"value" jsonb   ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "Site.name_unique" ON "public"."Site"("name")

CREATE UNIQUE INDEX "SiteNodeId" ON "public"."SiteNode"("siteId", "key", "parentNodeId")

ALTER TABLE "public"."Site" ADD FOREIGN KEY("ownerId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."SiteNode" ADD FOREIGN KEY("siteId")REFERENCES "public"."Site"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."SiteNode" ADD FOREIGN KEY("parentNodeId")REFERENCES "public"."SiteNode"("id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201020033940-cliptags-userbio..20201112083848-sites-data
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -46,8 +46,31 @@
   user        User     @relation(fields: [userId], references: [id])
   message     String
 }
+// Cloud 2
+
+model Site {
+  id          Int      @id @default(autoincrement())
+  name String @unique
+  ownerId Int
+  owner        User     @relation(fields: [ownerId], references: [id])
+}
+
+model SiteNode {
+  id          Int      @id @default(autoincrement())
+  key String
+  siteId Int
+  site        Site     @relation(fields: [siteId], references: [id])
+  parentNodeId        Int?
+  parentNode        SiteNode?     @relation(fields: [parentNodeId], references: [id])
+  schema Json?
+  value Json?
+  @@unique([siteId, key, parentNodeId], name: "SiteNodeId")
+}
+
+// ClipClock
+
 model Clip {
   id          Int       @id @default(autoincrement())
   draftTime   DateTime  @default(now())
   publishTime DateTime?
```


