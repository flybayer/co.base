# Migration `20201114042833-cleanup-pw-col`

This migration has been generated by Eric Vicenti at 11/13/2020, 8:28:34 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "User" DROP COLUMN "passwordSalt"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201112084449-owner-sites..20201114042833-cleanup-pw-col
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -12,9 +12,8 @@
   username            String    @unique
   email               String?   @unique
   phone               String?   @unique
   name                String?
-  passwordSalt        String?
   passwordHash        String?
   stripeCustomerId    String?
   giftedAccess        Int       @default(0)
   subscribedAccess    Int       @default(0)
@@ -22,9 +21,9 @@
   publicBio           String?
   Comment             Comment[]
   Clip                Clip[]
-  Sites               Site[]
+  Sites Site[]
 }
 model EmailValidation {
   id        Int      @id @default(autoincrement())
@@ -51,23 +50,26 @@
 // Cloud 2
 model Site {
-  id          Int    @id @default(autoincrement())
-  name        String @unique
-  ownerId     Int
-  owner       User   @relation(fields: [ownerId], references: [id])
+  id       Int        @id @default(autoincrement())
+  name     String     @unique
+  ownerId  Int
+  owner    User       @relation(fields: [ownerId], references: [id])
+  SiteNode SiteNode[]
 }
 model SiteNode {
-  id            Int       @id @default(autoincrement())
-  key           String
-  siteId        Int
-  site          Site      @relation(fields: [siteId], references: [id])
-  parentNodeId  Int?
-  parentNode    SiteNode? @relation(fields: [parentNodeId], references: [id])
-  schema        Json?
-  value         Json?
+  id           Int       @id @default(autoincrement())
+  key          String
+  siteId       Int
+  site         Site      @relation(fields: [siteId], references: [id])
+  parentNodeId Int?
+  parentNode   SiteNode? @relation(fields: [parentNodeId], references: [id])
+  schema       Json?
+  value        Json?
+
+  SiteNode SiteNode[] @relation("SiteNodeToSiteNode")
   @@unique([siteId, key, parentNodeId], name: "SiteNodeId")
 }
 // ClipClock
```


