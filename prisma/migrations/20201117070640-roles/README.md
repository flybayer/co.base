# Migration `20201117070640-roles`

This migration has been generated by Eric Vicenti at 11/16/2020, 11:06:40 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "NodeRole" (
"id" SERIAL,
    "siteNodeId" INTEGER NOT NULL,
    "userId" INTEGER NOT NULL,
    "name" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

CREATE TABLE "SiteRole" (
"id" SERIAL,
    "siteId" INTEGER NOT NULL,
    "userId" INTEGER NOT NULL,
    "name" TEXT NOT NULL,

    PRIMARY KEY ("id")
)

ALTER TABLE "NodeRole" ADD FOREIGN KEY("siteNodeId")REFERENCES "SiteNode"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "NodeRole" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "SiteRole" ADD FOREIGN KEY("siteId")REFERENCES "Site"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "SiteRole" ADD FOREIGN KEY("userId")REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201116185312-account-emails..20201117070640-roles
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -24,8 +24,10 @@
   Sites           Site[]
   EmailValidation EmailValidation[]
   VerifiedEmail   VerifiedEmail[]
+  SiteRole        SiteRole[]
+  NodeRole        NodeRole[]
 }
 model EmailValidation {
   id        Int      @id @default(autoincrement())
@@ -61,13 +63,15 @@
 // Cloud 2
 model Site {
-  id       Int        @id @default(autoincrement())
-  name     String     @unique
-  ownerId  Int
-  owner    User       @relation(fields: [ownerId], references: [id])
+  id      Int    @id @default(autoincrement())
+  name    String @unique
+  ownerId Int
+  owner   User   @relation(fields: [ownerId], references: [id])
+
   SiteNode SiteNode[]
+  SiteRole SiteRole[]
 }
 model SiteNode {
   id           Int       @id @default(autoincrement())
@@ -79,11 +83,32 @@
   schema       Json?
   value        Json?
   SiteNode SiteNode[] @relation("SiteNodeToSiteNode")
+
+  NodeRole NodeRole[]
+
   @@unique([siteId, key, parentNodeId], name: "SiteNodeId")
 }
+model NodeRole {
+  id         Int      @id @default(autoincrement())
+  siteNodeId Int
+  siteNode   SiteNode @relation(fields: [siteNodeId], references: [id])
+  userId     Int
+  user       User     @relation(fields: [userId], references: [id])
+  name       String
+}
+
+model SiteRole {
+  id     Int    @id @default(autoincrement())
+  siteId Int
+  site   Site   @relation(fields: [siteId], references: [id])
+  userId Int
+  user   User   @relation(fields: [userId], references: [id])
+  name   String
+}
+
 // ClipClock
 model Clip {
   id          Int       @id @default(autoincrement())
```


