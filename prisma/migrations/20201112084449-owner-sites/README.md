# Migration `20201112084449-owner-sites`

This migration has been generated by Eric Vicenti at 11/12/2020, 12:44:49 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201112083848-sites-data..20201112084449-owner-sites
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -21,8 +21,10 @@
   subscriptionEndTime DateTime?
   publicBio           String?
   Comment             Comment[]
   Clip                Clip[]
+
+  Sites               Site[]
 }
 model EmailValidation {
   id        Int      @id @default(autoincrement())
@@ -49,23 +51,23 @@
 // Cloud 2
 model Site {
-  id          Int      @id @default(autoincrement())
-  name String @unique
-  ownerId Int
-  owner        User     @relation(fields: [ownerId], references: [id])
+  id          Int    @id @default(autoincrement())
+  name        String @unique
+  ownerId     Int
+  owner       User   @relation(fields: [ownerId], references: [id])
 }
 model SiteNode {
-  id          Int      @id @default(autoincrement())
-  key String
-  siteId Int
-  site        Site     @relation(fields: [siteId], references: [id])
-  parentNodeId        Int?
-  parentNode        SiteNode?     @relation(fields: [parentNodeId], references: [id])
-  schema Json?
-  value Json?
+  id            Int       @id @default(autoincrement())
+  key           String
+  siteId        Int
+  site          Site      @relation(fields: [siteId], references: [id])
+  parentNodeId  Int?
+  parentNode    SiteNode? @relation(fields: [parentNodeId], references: [id])
+  schema        Json?
+  value         Json?
   @@unique([siteId, key, parentNodeId], name: "SiteNodeId")
 }
 // ClipClock
```


